# 知途（AI 成长规划智能体）0→1 实施指导文档

> 版本：v0.1  
> 依据：当前两份项目材料《项目总纲》《关于知途所有构想》  
> 目标：把“知途”从产品概念推进到可演示、可测试、可迭代的 0→1 MVP，并为后续 Web + App 完整产品打下统一的数据与智能体基础。  
> 注意：本文中凡涉及“建议”“阶段划分”“MVP 收敛方式”的内容，属于基于现有材料做出的实施推导；涉及产品宗旨、功能框架、Web/App 分工、技术栈等内容，则直接继承自现有材料。

---

## 0. 先统一：这个项目到底要做成什么

知途不是“AI 版 Todo List + 聊天框”，也不是一次性生成计划的工具。

它要维护的是一个**持续变化的成长状态（Growth State / Plan State）**：

```text
理解用户
  ↓
明确目标
  ↓
构建成长路径
  ↓
拆解阶段与任务
  ↓
陪伴用户执行
  ↓
感知真实执行状态
  ↓
发现计划与现实偏差
  ↓
动态重新规划
  ↓
沉淀成长记录
  ↓
进一步理解用户
  ↺
```

产品真正管理的不是“任务”，而是：

> 一个人在目标、现实与时间约束之下不断变化的成长过程。

因此，所有产品、技术、UI、Agent 设计都必须服务于一个核心闭环：

```text
Conversation ↔ Growth Space ↔ Execution
```

并且必须满足下面四个原则：

1. **计划是结构化状态，不是一段文本。**
2. **Graph / Timeline / Plan 是同一个 Plan State 的不同投影。**
3. **用户可以通过语言修改计划，也可以直接操作界面修改计划。**
4. **AI 不只是 Plan，更重要的是根据现实持续 Replan。**

---

# 第一部分：0→1 阶段必须先回答的五个问题

## 1. 用户是谁

当前材料已经明确：0→1 测试用户优先锁定为**大学生**。

第一阶段不要把目标用户扩展到“所有需要成长的人”，因为这会导致：

- 目标类型过多；
- 规划逻辑无法稳定；
- 用户画像字段无限膨胀；
- Agent 无法判断什么叫“合理计划”；
- 测试反馈失去可比性。

### MVP 推荐优先测试的大学生目标类型

建议只选 3 类：

1. **考试 / 证书类**
   - 四六级
   - 考研
   - 教资
   - 计算机等级考试

2. **技能成长类**
   - 学 Python
   - 学前端
   - 学设计
   - 学英语口语

3. **项目 / 求职类**
   - 做一个作品集
   - 完成比赛项目
   - 准备实习
   - 准备秋招

这三类共同具备：

- 有相对明确的终点；
- 有时间约束；
- 可以拆解为阶段；
- 可以形成执行记录；
- 用户容易产生拖延和偏差；
- 适合测试“动态重规划”。

### 第一阶段暂缓

以下类型不建议一开始进入 MVP：

- “我要变得更自信”
- “我要变得更自律”
- “我要找到人生方向”
- 长期情绪管理
- 复杂职业生涯决策

不是因为这些需求没有价值，而是它们很难在早期形成清晰、可验证的规划与执行闭环。

---

## 2. MVP 要证明什么

0→1 阶段不要追求“功能完整”，而要证明核心范式成立。

MVP 必须证明以下 5 件事：

### 证明 1：AI 能把模糊目标变成可执行的成长路径

示例：

```text
用户：
我想三个月把英语六级提到 500 分。

系统：
先理解：
- 当前成绩
- 薄弱项
- 每周可投入时间
- 考试日期
- 学习资源
- 课程冲突
- 用户习惯

然后生成：
Goal
 ├─ Stage 1 基础诊断
 ├─ Stage 2 词汇和听力强化
 ├─ Stage 3 阅读写作强化
 └─ Stage 4 模考冲刺
```

---

### 证明 2：用户和 AI 可以共同修改同一份 Growth Space

不是：

```text
左边聊天
右边显示一份生成完就结束的计划
```

而是：

```text
用户说：
“我周三实验课很多，那个任务放不下。”

AI：
修改 Timeline 和对应 Task。

用户也可以：
直接拖动节点 / 修改日期 / 删除任务。

系统：
把这个修改理解成新的现实约束。
```

---

### 证明 3：系统能感知执行偏差

至少支持：

- 完成；
- 未完成；
- 延期；
- 放弃；
- 部分完成；
- 用户主动反馈原因。

例如：

```text
计划：
周一背 100 个单词。

现实：
只完成 30 个。

系统不能只记录：
70% 未完成。

系统应该继续理解：
为什么？
- 时间不足？
- 难度过高？
- 临时有课？
- 任务本身拆分不合理？
```

---

### 证明 4：系统会 Replan

Replan 不是简单把任务顺延一天。

它需要重新考虑：

```text
目标是否仍成立
剩余时间
已完成内容
能力变化
近期执行能力
新的时间约束
任务依赖
阶段优先级
```

然后更新同一份 Plan State。

---

### 证明 5：系统能让用户看到“成长结果”

用户最终不仅看到：

```text
本周完成 13 个任务
```

还应该逐步看到：

- 哪些能力在变化；
- 哪些行为模式反复出现；
- 哪些目标正在接近；
- 哪些执行策略对自己有效；
- AI 对自己的理解发生了什么变化。

---

# 第二部分：先定义整个系统的唯一“真相源”

## 3. 为什么必须先设计 Growth State

整个项目最容易出现的架构错误是：

```text
聊天记录有一套状态
Graph 有一套状态
Timeline 有一套状态
Todo 有一套状态
App 又有一套状态
```

最后会导致：

- Graph 改了，Timeline 没改；
- AI 说任务延期了，数据库还是旧日期；
- App 显示旧计划；
- 重规划以后无法追溯为什么变化；
- 用户不知道哪个页面才是真的。

因此，系统必须建立一个唯一的结构化状态：

# Growth State

它是整个产品的核心数据模型。

---

## 4. 推荐的数据层级

```text
User
│
├── UserProfile
├── Memory
├── Constraints
├── Preferences
│
├── Goal
│   │
│   ├── Stage
│   │   │
│   │   ├── Task
│   │   └── Milestone
│   │
│   └── GoalMetric
│
├── ExecutionRecord
├── Reflection / Note
├── Insight
└── GrowthAsset
```

---

## 5. 最低可用数据模型

### User

```ts
User {
  id
  nickname
  timezone
  created_at
}
```

### UserProfile

```ts
UserProfile {
  user_id
  education_stage
  major
  grade
  available_time_pattern
  learning_preferences
  self_reported_strengths
  self_reported_weaknesses
  long_term_interests
  updated_at
}
```

注意：

用户画像不应该是一次问卷后永久固定。

它应该随着执行记录变化。

---

### Goal

```ts
Goal {
  id
  user_id
  title
  description
  category
  target_date
  status

  motivation
  success_definition

  created_at
  updated_at
}
```

---

### Stage

```ts
Stage {
  id
  goal_id
  title
  description

  sequence
  start_date
  end_date

  status
}
```

---

### Task

```ts
Task {
  id
  goal_id
  stage_id

  title
  description

  planned_start
  planned_end
  estimated_minutes

  priority
  status

  dependency_ids

  source
  source_reason

  created_at
  updated_at
}
```

其中：

```text
source
```

至少区分：

- AI_CREATED
- USER_CREATED
- AI_REPLAN
- USER_EDITED

而：

```text
source_reason
```

必须记录：

> 为什么这个任务会存在 / 为什么它被改变。

这是后续“可解释规划”的关键。

---

### ExecutionRecord

```ts
ExecutionRecord {
  id
  task_id
  user_id

  actual_start
  actual_end

  completion_ratio

  result

  delay_reason

  user_feedback

  created_at
}
```

---

### PlanVersion

这是非常关键的一张表。

```ts
PlanVersion {
  id
  goal_id

  version

  trigger_type
  trigger_detail

  snapshot

  created_at
}
```

例如：

```text
V1
AI 初始规划

V2
用户修改周三课程约束

V3
连续三天未完成任务

V4
距离考试只剩 30 天
```

以后任何 Replan 都应该生成新的 PlanVersion。

不要直接覆盖旧计划而不留历史。

---

# 第三部分：设计 Agent，而不是设计一个超级 Prompt

## 6. Agent 层整体结构

现有材料已经确定 openJiuwen 作为 Agent 框架。

建议不要从第一天就做十几个 Agent。

0→1 最少拆成 4 个逻辑角色即可：

```text
                   Orchestrator
                       │
        ┌──────────────┼──────────────┐
        │              │              │
User Understanding   Planner       Replanner
        │              │              │
        └──────────────┼──────────────┘
                       │
                    Memory
```

后期再拆：

```text
Coach
Reporter
Knowledge Agent
Behavior Analyst
```

---

## 7. Orchestrator

职责：

> 判断用户当前这句话，需要系统做什么。

例如：

```text
“我今天好累，不想学了”
```

可能不是：

```text
修改 Goal
```

而是：

```text
更新状态
↓
判断今天负荷
↓
Coach 回复
↓
必要时建议调整任务
```

而：

```text
“我决定不考研了”
```

则属于高影响操作：

```text
Goal State Change
```

不能直接自动执行。

### 推荐输出

```json
{
  "intent": "EXECUTION_FEEDBACK",
  "target_goal_id": "...",
  "requires_plan_change": true,
  "requires_confirmation": false
}
```

---

# 8. User Understanding Agent

它负责理解：

```text
这个人是谁
↓
他想要什么
↓
为什么想要
↓
现实约束是什么
↓
成功标准是什么
```

输出不能只是自然语言总结。

需要结构化：

```json
{
  "goal": {},
  "constraints": [],
  "available_time": {},
  "known_risks": [],
  "missing_information": []
}
```

### 一个重要规则

信息不足时：

**先问，不要假装知道。**

例如用户说：

```text
“我要两个月学会前端。”
```

系统至少需要进一步了解：

- 当前基础；
- “学会”是什么意思；
- 每周可投入时间；
- 最终交付目标；
- 是否已有课程资源。

---

# 9. Planner Agent

Planner 不直接生成“周一到周日 Todo”。

推荐四步规划：

```text
Step 1 Goal Clarification
       ↓
Step 2 Strategy
       ↓
Step 3 Stage Decomposition
       ↓
Step 4 Task Scheduling
```

---

## Step 1：Goal Clarification

输出：

```text
目标
成功标准
截止日期
限制条件
风险
```

---

## Step 2：Strategy

回答：

> 到达目标的整体路径是什么？

例如：

```text
六级 500
↓
诊断当前水平
↓
建立词汇基础
↓
强化听力阅读
↓
专项写作
↓
模考
```

---

## Step 3：Stage

把策略变成阶段。

---

## Step 4：Task Scheduling

最后才安排具体任务。

避免一开始就：

```text
9 月 17 日背单词
9 月 18 日做听力
...
```

否则 AI 很容易生成看似详细、实际上没有逻辑基础的计划。

---

# 10. Replanner Agent

这是知途区别于普通 AI 计划软件的核心。

### Replan Trigger

至少设计：

```text
任务未完成
任务提前完成
用户修改可用时间
目标变化
截止日期变化
连续拖延
执行效率明显变化
用户主动要求调整
```

---

## Replan 决策顺序

不要：

```text
未完成
↓
全部延期
```

正确顺序：

```text
发生了什么？
↓
是偶发事件还是稳定变化？
↓
原计划哪里失效？
↓
目标是否变化？
↓
约束是否变化？
↓
优先级是否变化？
↓
哪些任务必须保留？
↓
哪些任务可以压缩 / 删除 / 延后？
↓
生成新 PlanVersion
```

---

# 第四部分：Growth Space 怎么做

## 11. Conversational Growth Space

Web 的核心不是 Dashboard。

应该是一个：

> 可以通过自然语言和直接操作共同编辑的成长空间。

推荐布局：

```text
┌─────────────────────────────────────────────────────┐
│ Top Bar                                             │
├───────────────────────┬─────────────────────────────┤
│                       │                             │
│                       │ Conversation                │
│   Growth Space        │                             │
│                       │ AI explanation              │
│ Graph / Timeline      │ Questions                   │
│ / Plan                │ Suggestions                 │
│                       │                             │
├───────────────────────┴─────────────────────────────┤
│ Context / selected node / version / actions         │
└─────────────────────────────────────────────────────┘
```

重点不是左右比例，而是：

> Conversation 和 Plan State 必须同时可见、双向联动。

---

# 12. Graph View

回答：

> 为什么这样做？

展示：

```text
Goal
│
├── Stage
│   ├── Milestone
│   └── Task
│
└── Dependency
```

也可以表现：

```text
能力 A
↓
能力 B
↓
目标
```

Graph 里至少需要：

- 节点选择；
- 节点展开；
- 节点编辑；
- 节点删除；
- 查看原因；
- 查看依赖；
- AI 修改动画。

技术上可使用现有材料中指定的 React Flow。

---

# 13. Timeline View

回答：

> 什么时候做？

时间线必须读取**同一份 Task / Stage 数据**。

不要再建一张“TimelineTask”表。

推荐：

```text
2026-09
│
├── Stage 1
│      ├── Task A
│      └── Task B
│
├── Milestone
│
└── Stage 2
```

必须支持：

- 拖动日期；
- 缩放；
- 阶段区间；
- 里程碑；
- 计划与实际对比。

---

# 14. Plan View

回答：

> 具体做什么？

Plan View 更接近执行清单，但不能退化为普通 Todo。

任务最好展示：

```text
任务标题
所属目标
所属阶段
为什么做
预计时间
截止时间
依赖
状态
AI 建议
```

---

# 第五部分：Today 应该怎么做

## 15. Today 不是“今天所有任务”

Today 应回答：

> 今天最值得把注意力放在哪里？

推荐结构：

```text
Good morning
↓
当前核心目标
↓
Today's Focus
↓
最重要 1~3 个任务
↓
执行状态
↓
AI Coach Insight
```

---

## 16. Today Selection Logic

MVP 可以先不用机器学习。

先用可解释规则：

```text
priority_score =
goal_priority
+ deadline_pressure
+ dependency_importance
+ overdue_pressure
- estimated_load_penalty
```

这里不建议一开始设置看似精确但没有数据支撑的复杂权重。

MVP 第一版甚至可以直接使用离散等级：

```text
High
Medium
Low
```

并让 Agent 综合判断。

---

# 第六部分：执行反馈系统

## 17. 任务状态不要只有 Done / Not Done

推荐：

```text
TODO
IN_PROGRESS
DONE
PARTIAL
DELAYED
SKIPPED
CANCELLED
```

特别重要的是：

```text
PARTIAL
DELAYED
SKIPPED
```

因为这些状态才是 Replan 的主要输入。

---

## 18. 执行反馈

用户完成任务后，尽量不要让他填长表单。

推荐快速反馈：

```text
完成了吗？
✓ 完成
◐ 部分完成
→ 延后
× 放弃
```

只有需要时才继续追问：

```text
发生了什么？
```

可选：

- 时间不够；
- 太难；
- 状态不好；
- 临时有事；
- 任务不合理；
- 其他。

---

# 第七部分：Memory 怎么做

## 19. Memory 分层

不要把所有聊天记录全部丢给模型。

建议拆成：

```text
Short-term Context
Conversation Summary
User Profile
Behavior Pattern
Goal Memory
Growth History
```

---

## 20. 什么值得成为长期记忆

可以进入：

- 稳定偏好；
- 长期时间约束；
- 反复出现的行为规律；
- 用户明确表达的重要信息；
- 对规划有长期影响的信息。

不要轻易进入：

- 一次性情绪；
- 无关闲聊；
- 暂时性事件；
- AI 自己猜测的性格结论。

---

# 第八部分：随笔系统

## 21. 随笔的真正作用

随笔不是第二个 Notion。

它是：

> Growth State 的低摩擦现实输入。

输入：

```text
文字
图片
短句
经历
想法
```

AI 后台分析：

```text
是否与某个 Goal 有关
是否代表新的 Constraint
是否包含新的 Achievement
是否影响 User Profile
是否值得形成 Memory
```

然后让用户确认。

---

# 第九部分：成长模块

## 22. 早期不要追求复杂“AI 人格分析”

MVP 的成长报告应该先使用真实可验证数据。

例如：

### Weekly Report

```text
本周目标
完成任务
延期任务
计划变更
累计投入时间
主要偏差原因
AI 观察
下周调整
```

---

## 23. Growth Asset

成长资产建议先定义成：

> 用户通过成长过程形成、可以被重复利用或展示的成果。

例如：

- 完成的项目；
- 作品；
- 掌握的技能；
- 证书；
- 学习笔记；
- 阶段成果；
- 里程碑。

不要在 MVP 阶段把 Growth Asset 定义得过度抽象。

---

# 第十部分：Web / App 分工

## 24. Web = Think / Plan

负责：

```text
复杂目标规划
Growth Space
Graph
Timeline
Plan
长对话
深度分析
报告
用户画像
```

技术：

```text
Next.js
React
TypeScript
React Flow
Tailwind CSS
Framer Motion
```

---

# 25. App = Act / Sense

负责：

```text
Today
当前任务
完成反馈
快速对话
随笔
通知
执行反馈
行为感知
主动干预
```

技术：

```text
Flutter
Dart
```

---

# 第十一部分：后端架构

## 26. 推荐总体架构

```text
                    ┌─────────────┐
                    │    Web      │
                    │ Next.js     │
                    └──────┬──────┘
                           │
                           │ HTTP / WebSocket
                           │
                    ┌──────▼──────┐
                    │   FastAPI   │
                    │  Business   │
                    └──────┬──────┘
                           │
          ┌────────────────┼────────────────┐
          │                │                │
┌─────────▼──────┐ ┌───────▼────────┐ ┌────▼───────┐
│ openJiuwen     │ │ PostgreSQL     │ │ Redis      │
│ Agent Layer    │ │ Persistent     │ │ Realtime   │
└────────────────┘ └────────────────┘ └────────────┘
                           ▲
                           │
                    ┌──────┴──────┐
                    │ Flutter App │
                    └─────────────┘
```

---

# 27. 后端模块

建议拆：

```text
/auth
/users
/profile
/goals
/stages
/tasks
/executions
/plans
/conversations
/memory
/insights
/reports
/agent
```

---

# 28. 不要让 Agent 直接写数据库

推荐流程：

```text
User
↓
API
↓
Agent
↓
Structured Action
↓
Validation
↓
Business Service
↓
Database
```

例如 AI 返回：

```json
{
  "action": "MOVE_TASK",
  "task_id": "123",
  "new_date": "2026-09-20",
  "reason": "用户周三新增实验课"
}
```

Backend 再验证：

```text
Task 是否存在
User 是否有权限
日期是否合法
是否影响依赖
是否需要确认
```

然后写数据库。

这比让大模型直接执行 SQL 安全得多，也更容易调试。

---

# 第十二部分：事件系统

## 29. 建议建立 Domain Event

因为 Replan、Memory、Report 都依赖事件。

例如：

```text
TASK_COMPLETED
TASK_DELAYED
TASK_SKIPPED
TASK_EDITED

GOAL_CREATED
GOAL_CHANGED

NOTE_CREATED

PLAN_CREATED
PLAN_REPLANNED
```

以后：

```text
TASK_DELAYED
↓
Behavior Analyzer
↓
Replan Evaluator
↓
可能触发 REPLAN
```

---

# 第十三部分：API 设计

## 30. 第一批 API

### Goal

```text
POST   /goals
GET    /goals
GET    /goals/{id}
PATCH  /goals/{id}
```

### Plan

```text
POST /goals/{id}/plan
POST /goals/{id}/replan
GET  /goals/{id}/plan
GET  /goals/{id}/versions
```

### Task

```text
GET   /tasks/today
PATCH /tasks/{id}
POST  /tasks/{id}/execution
```

### Conversation

```text
POST /conversations/{id}/messages
```

### Note

```text
POST /notes
GET  /notes
```

---

# 第十四部分：从 0 到 1 的实际开发顺序

以下阶段划分属于实施建议，而不是原材料中的固定排期。

---

# Phase 0：项目冻结与统一语义

## 目标

所有人先统一：

```text
什么是 Goal
什么是 Stage
什么是 Task
什么是 Growth State
什么是 Replan
什么是 Memory
什么是 Growth Asset
```

### 输出物

```text
/docs
  product-principles.md
  domain-model.md
  agent-contract.md
  api-contract.md
```

### 完成标准

团队任何两个人都能对同一个案例画出基本一致的数据结构。

---

# Phase 1：先把非 AI 骨架做出来

## 目标

先证明：

```text
Growth State
↓
Graph
Timeline
Plan
```

三种视图能同步。

### 必须完成

Web：

- 创建 Goal；
- Stage；
- Task；
- Graph；
- Timeline；
- Plan；
- 手动编辑；
- 自动同步。

Backend：

- Goal CRUD；
- Stage CRUD；
- Task CRUD；
- PlanVersion；
- PostgreSQL。

### 此阶段暂时不要做

- 高级 Agent；
- 自动行为感知；
- 多 Agent；
- 精美报告；
- 复杂 App。

---

# Phase 2：接入 AI 规划

## 用户流程

```text
用户输入目标
↓
AI 追问
↓
目标澄清
↓
生成 Strategy
↓
生成 Stage
↓
生成 Task
↓
写入 Growth State
↓
Graph / Timeline / Plan 同时出现
```

### 完成标准

至少 10 个大学生典型目标可以生成：

- 结构完整；
- 时间基本合理；
- 任务有来源；
- 用户能继续编辑。

---

# Phase 3：实现 Conversational Growth Space

这是第一次真正验证你们的创新点。

## 必须完成

```text
用户说：
“把英语听力放到晚上。”

AI
↓
识别目标
↓
生成结构化修改 action
↓
Backend 修改数据
↓
Timeline 动画变化
```

同时支持反向：

```text
用户拖动 Timeline
↓
Backend 更新
↓
Conversation Context 知道计划已经改变
```

---

# Phase 4：Today + Execution

现在开始形成执行闭环。

App 可以从这一阶段进入。

## Web / App 共用数据

```text
GET /tasks/today
```

App 第一版只做：

```text
Today
Task
Complete
Delay
Skip
Quick Chat
```

不要一开始复制完整 Growth Space。

---

# Phase 5：Replan

建立真正核心壁垒。

### 示例测试

原计划：

```text
D1 2h
D2 2h
D3 2h
```

用户实际：

```text
D1 0h
D2 0.5h
D3 0h
```

系统需要判断：

```text
偶发？
持续？
计划过重？
时间约束变化？
```

然后：

```text
生成 Replan Proposal
```

### 高影响调整建议确认

例如：

```text
删除整个 Stage
改变 Goal
大幅降低成功标准
延期最终目标
```

必须要求用户确认。

低影响：

```text
移动一个普通任务
减小今日任务量
```

可以根据用户授权自动执行。

---

# Phase 6：Memory + 用户画像

现在系统才真正开始“越来越懂用户”。

先建立有限的 Profile：

```text
时间规律
学习偏好
典型拖延原因
任务负荷水平
计划完成趋势
```

不要直接输出：

```text
你是一个 XX 性格的人。
```

优先输出可验证行为结论。

---

# Phase 7：成长报告

完成：

```text
Weekly Report
Milestone
Growth Asset
Execution Trend
```

做到这里，整个闭环已经完整：

```text
Plan
↓
Execute
↓
Sense
↓
Replan
↓
Learn
↓
Report
```

---

# 第十五部分：建议的 0→1 MVP 范围

## 必须有

### Web

- 登录；
- Goal；
- Conversation；
- Growth Space；
- Graph；
- Timeline；
- Plan；
- AI 创建计划；
- AI 修改计划；
- 手动修改；
- Plan Version。

### App

- Today；
- 当前 Task；
- Complete；
- Delay；
- Skip；
- Quick Chat；
- Quick Note。

### Agent

- User Understanding；
- Planner；
- Replanner；
- Memory 基础能力。

---

## 可以第二阶段再做

- 高级行为感知；
- 自动读取手机使用情况；
- 多 Agent 大规模协作；
- 强人格化 Coach；
- 复杂成长资产；
- 社交；
- 排行榜；
- Gamification；
- 高级数据可视化。

---

# 第十六部分：主动能力设计

## 31. 主动能力不能等于“多发通知”

当前材料定义：

```text
被动模式
陪伴模式
教练模式
严格模式
```

实现时建议拆成权限：

```text
can_message_proactively
can_suggest_plan_change
can_auto_adjust_low_risk_task
can_criticize
can_remind_rest
```

这样用户能真正控制 Agent 行为。

---

# 第十七部分：需要优先解决的产品逻辑漏洞

下面这些是根据现有构想推演后，必须提前堵住的问题。

---

## 漏洞 1：AI “理解用户”可能变成无限问答

解决：

建立 Minimal Planning Context。

例如创建目标最低只要求：

```text
目标
成功标准
截止日期
当前水平
每周可用时间
主要限制
```

信息达到最低要求就开始规划。

以后边执行边理解。

---

## 漏洞 2：规划可能看起来很聪明，但现实不可执行

解决：

每个 Plan 必须经过 Constraint Check：

```text
总可用时间
vs
总预计任务时间
```

若：

```text
需求时间 > 用户可用时间
```

AI 必须：

- 延长目标；
- 降低范围；
- 调整成功标准；
- 或明确指出不可行。

不能硬生成计划。

---

## 漏洞 3：用户每天都在修改计划，系统失去稳定性

解决：

区分：

```text
micro adjustment
vs
structural replan
```

例如：

```text
任务改到晚上
```

不是一次完整 Replan。

而：

```text
考试日期提前一个月
```

才触发结构级 Replan。

---

## 漏洞 4：AI 太主动会让用户反感

解决：

主动能力必须：

```text
用户授权
+
频率控制
+
影响等级
```

且默认应保守。

---

## 漏洞 5：AI 画像容易过度推断

解决：

每个 Insight 记录：

```text
evidence
confidence
source
```

例如：

```json
{
  "insight": "用户晚上完成学习任务的概率更高",
  "evidence": "过去14天中，晚间任务完成率高于上午",
  "confidence": 0.72
}
```

而不是：

```text
你是夜猫子型人格。
```

---

# 第十八部分：规划可行性校验

## 32. 时间预算

假设：

```text
用户每周可投入 = 10 小时
目标剩余 = 8 周
```

理论总预算：

```text
10 × 8 = 80 小时
```

如果 Planner 输出：

```text
100 个任务
每个 1 小时
```

总需求：

```text
100 小时
```

那么：

```text
100 > 80
```

计划不可行。

系统必须调整。

---

## 推荐公式

```text
AvailableCapacity
=
WeeklyAvailableMinutes
× RemainingWeeks
× SafetyFactor
```

建议 SafetyFactor 第一版作为保守系数使用，例如：

```text
0.7 ~ 0.85
```

这是产品策略参数，不是材料中既定数值。

例如：

```text
每周 600 分钟
剩余 8 周
SafetyFactor = 0.8

Capacity
=
600 × 8 × 0.8
=
3840 分钟
=
64 小时
```

所以规划任务总预计时间最好不要超过约 64 小时。

---

# 第十九部分：测试体系

## 33. 先建测试案例库

至少建立：

```text
/tests/cases
```

每个测试案例：

```yaml
persona:
goal:
deadline:
current_level:
weekly_time:
constraints:
expected_strategy:
expected_risks:
```

---

## 34. 建议第一批 10 个案例

例如：

```text
1. 六级 430 → 500
2. 零基础 Python → 做课程项目
3. 两个月完成前端作品
4. 三个月准备教资
5. 一个半月准备数学竞赛
6. 一学期完成科研入门
7. 六周准备实习面试
8. 30 天完成作品集
9. 两个月减轻课程挂科风险
10. 一个月完成比赛项目
```

---

# 第二十部分：Agent 评估指标

## Planner

评价：

```text
Goal clarity
Stage logic
Task feasibility
Constraint satisfaction
Dependency correctness
Time feasibility
```

---

## Replanner

评价：

```text
是否理解偏差原因
是否避免无脑顺延
是否保留核心目标
是否考虑剩余时间
是否保持结构一致性
```

---

## Coach

评价：

```text
是否过度打扰
是否真正帮助行动
是否与用户授权一致
```

---

# 第二十一部分：数据指标

0→1 不要先看 DAU。

优先看产品闭环指标。

### Activation

```text
用户是否创建第一个 Goal
是否完成第一次规划
```

### Plan Adoption

```text
用户是否接受 / 修改计划
```

### Execution

```text
是否开始执行 Task
```

### Replan

```text
发生偏差后是否继续使用系统
```

### Growth Loop

```text
7 天内是否完成：
Plan → Execute → Feedback → Replan
```

这是比“打开 App 几次”更重要的早期指标。

---

# 第二十二部分：代码仓库建议

```text
zhitu/
│
├── apps/
│   ├── web/
│   └── mobile/
│
├── services/
│   ├── api/
│   └── agent/
│
├── packages/
│   ├── domain/
│   ├── schemas/
│   └── ui/
│
├── docs/
│
├── tests/
│
└── infra/
```

如果团队早期人数很少，也可以暂时不做复杂 Monorepo 工具链，但目录边界建议从一开始清楚。

---

# 第二十三部分：Git 协作

材料里已经提出：

> 同一个 GitHub 项目，不同 branch，两到三天融合到 main。

建议：

```text
main
develop
feature/*
```

但更重要的不是 branch 数量，而是：

```text
小 PR
高频 merge
避免一人开发两周再合并
```

每次 Pull Request 至少检查：

```text
是否修改 Schema
是否影响 Growth State
是否影响 API
是否影响 Agent Contract
是否有 Migration
```

---

# 第二十四部分：最关键的 Agent Contract

所有 Agent 最终不要只返回文本。

统一：

```json
{
  "message": "...",
  "actions": [],
  "state_patch": {},
  "requires_confirmation": false,
  "reasoning_summary": "...",
  "warnings": []
}
```

注意：

这里的 `reasoning_summary` 是面向产品的**简要可解释说明**，不是模型内部推理过程。

例如：

```json
{
  "message": "考虑到你周三增加了实验课，我建议把听力训练移到周四。",
  "actions": [
    {
      "type": "MOVE_TASK",
      "task_id": "task_12",
      "to": "2026-09-18"
    }
  ],
  "requires_confirmation": false,
  "reasoning_summary": "周三可用学习时间由2小时下降到30分钟。"
}
```

---

# 第二十五部分：安全边界

## 高影响操作

必须确认：

- 删除 Goal；
- 删除 Stage；
- 大范围删除 Task；
- 修改最终目标；
- 改目标截止日期；
- 放弃目标；
- 大幅降低目标。

---

## 低影响操作

用户授权后可自动：

- 今日任务顺序；
- 小幅时间调整；
- 提醒；
- 建议；
- 拆分任务。

---

# 第二十六部分：0→1 里程碑

## M0：Concept Locked

完成：

- 产品原则；
- Domain Model；
- Growth State；
- Agent Contract。

---

## M1：State Works

完成：

```text
Goal
Stage
Task
Graph
Timeline
Plan
```

可以同步。

---

## M2：AI Can Plan

完成：

```text
目标输入
↓
追问
↓
Plan
↓
写入 Growth State
```

---

## M3：Conversation Can Change State

完成：

```text
自然语言
↓
结构化 Action
↓
界面变化
```

---

## M4：Execution Works

完成：

```text
Today
Complete
Delay
Skip
```

---

## M5：Replan Works

完成：

```text
Execution deviation
↓
Replan
↓
PlanVersion
```

---

## M6：Learning Works

完成：

```text
Memory
User Profile
Weekly Report
```

到 M6，知途 0→1 产品闭环成立。

---

# 第二十七部分：推荐开发节奏

以下不是材料中的固定排期，而是一个可执行参考。

如果团队资源较少，建议按 10~14 周推进 MVP，而不是同时铺开所有功能。

示例：

```text
Week 1
Domain / UX / Architecture

Week 2-3
Backend + Growth State

Week 3-4
Graph / Timeline / Plan

Week 5
Planner

Week 6
Conversational Growth Space

Week 7
Today / Execution

Week 8
Mobile MVP

Week 9
Replan

Week 10
Memory

Week 11
Report

Week 12+
Testing / polish / demo
```

如果团队人数、能力结构或课程安排不同，应以里程碑而不是日历时间作为主要管理方式。

---

# 第二十八部分：最终 Demo 应该怎么演示

不要从“登录页面”开始演示。

直接使用一个典型用户：

```text
大二学生
六级 430
目标三个月 500
每周 8 小时
周三实验课
```

---

## Demo Flow

### 1

用户：

```text
我三个月后想把六级考到 500。
```

---

### 2

AI 追问必要信息。

---

### 3

Growth Space 实时生成：

```text
Goal
Stage
Task
```

Graph 出现。

---

### 4

切 Timeline。

显示完整三个月路径。

---

### 5

用户：

```text
周三我其实没时间。
```

Timeline 自动变化。

---

### 6

用户直接拖动任务。

聊天上下文理解变化。

---

### 7

进入 App：

```text
Today
```

完成 / 延期任务。

---

### 8

连续几天执行偏差。

---

### 9

AI：

```text
我发现原计划每天 90 分钟并不适合你目前的课程安排。
建议重新调整。
```

---

### 10

Replan。

Graph / Timeline / Plan 同时变化。

---

### 11

Weekly Report：

```text
不是：
你完成了 7 个任务。

而是：
你在晚间学习完成率明显更高，
因此下周核心学习任务将优先放到晚间。
```

这个 Demo 才真正能证明知途区别于普通 AI Todo。

---

# 第二十九部分：正式开发前必须完成的 Checklist

## Product

- [ ] 用户范围明确
- [ ] MVP 目标明确
- [ ] Growth State 定义完成
- [ ] Replan 定义完成
- [ ] 高风险操作定义完成
- [ ] Agent 主动权限定义完成

## Backend

- [ ] PostgreSQL Schema
- [ ] PlanVersion
- [ ] Event Model
- [ ] API Contract
- [ ] Agent Action Validation

## Web

- [ ] Growth Space
- [ ] Graph
- [ ] Timeline
- [ ] Plan
- [ ] Conversation
- [ ] 双向同步

## Mobile

- [ ] Today
- [ ] Task
- [ ] Execution Feedback
- [ ] Quick Chat
- [ ] Quick Note

## Agent

- [ ] Orchestrator
- [ ] User Understanding
- [ ] Planner
- [ ] Replanner
- [ ] Memory

## Test

- [ ] 10 个典型大学生场景
- [ ] 规划测试
- [ ] Replan 测试
- [ ] 状态一致性测试
- [ ] 高影响操作确认测试

---

# 第三十部分：逻辑与计算复核

本节用于检查本实施方案自身是否存在明显逻辑冲突或计算错误。

## 1. 产品逻辑一致性检查

### 检查项 A

```text
“计划是一个结构化状态”
```

与：

```text
Graph / Timeline / Plan 共用数据
```

一致。

通过。

### 检查项 B

```text
Web = Think / Plan
App = Act / Sense
```

与：

```text
Web 重点做 Growth Space
App 重点做 Today / Execution
```

一致。

通过。

### 检查项 C

```text
AI 主动
```

与：

```text
AI 不监督用户
```

并不冲突。

前提是主动能力用于：

- 管理注意力；
- 帮助执行；
- 根据授权调整计划；

而不是惩罚式监督。

通过。

### 检查项 D

```text
用户允许直接编辑计划
```

与：

```text
AI 维护 Plan State
```

存在潜在冲突。

解决方式：

> User Edit 和 AI Action 都必须进入同一个 Domain Event / PlanVersion 系统。

已在架构中解决。

---

## 2. 数据逻辑检查

Plan：

```text
Goal
→ Stage
→ Task
```

ExecutionRecord 只记录现实执行。

因此：

```text
计划数据 ≠ 执行数据
```

不会因为用户未完成任务而破坏原始计划历史。

通过。

---

## 3. 时间容量计算检查

示例：

```text
600 min/week
× 8 weeks
= 4800 min
```

乘：

```text
SafetyFactor 0.8
```

得到：

```text
4800 × 0.8
= 3840 min
```

换算：

```text
3840 ÷ 60
= 64 hours
```

计算正确。

---

## 4. Roadmap 顺序检查

顺序：

```text
Domain
↓
State
↓
Views
↓
Planner
↓
Conversation Editing
↓
Execution
↓
Replan
↓
Memory
↓
Report
```

依赖关系成立。

特别是：

```text
Replan
```

必须依赖：

```text
Plan + Execution
```

所以不能放在 Execution 之前。

通过。

---

# 结论

知途真正需要构建的，不是一组页面，也不是一个会生成计划的聊天机器人。

它的技术与产品核心应该是：

```text
                 User
                  │
                  ▼
             Conversation
                  │
                  ▼
          Structured Actions
                  │
                  ▼
             Growth State
        ┌─────────┼─────────┐
        │         │         │
      Graph    Timeline    Plan
        │         │         │
        └─────────┼─────────┘
                  │
               Execution
                  │
                  ▼
                Events
                  │
                  ▼
               Replan
                  │
                  ▼
               Memory
                  │
                  ▼
              Growth
```

一句话定义：

> **知途是一个和用户共同维护“持续变化的成长状态”的 AI 成长智能体。它通过理解、规划、执行、感知和重规划，让目标真正进入现实。**

真正的 0→1 成功标准不是“页面做完了”，而是至少有一名真实用户能够经历：

```text
我有一个目标
↓
AI 理解我
↓
我们一起形成计划
↓
我开始执行
↓
现实和计划发生偏差
↓
系统理解偏差
↓
计划重新变化
↓
系统因此更了解我
```

当这条循环第一次完整跑通时，知途才真正从“概念”变成了“产品”。
